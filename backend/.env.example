PORT=5000
NODE_ENV=development

# Gemini API Key (If omitted, Gemini Mock Service will be used)
GEMINI_API_KEY=

# Firebase Admin SDK Credentials (If omitted, Mock Firestore Database will be used)
# Provide as a single-line JSON string containing serviceAccount credentials
# Example: {"type":"service_account","project_id":"...",...}
FIREBASE_SERVICE_ACCOUNT=

# JWT Secret for local mock auth token generation
JWT_SECRET=fifa_world_cup_2026_stadium_guide_secret_key_9988
